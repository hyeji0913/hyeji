 //#include<stdio.h>

void(){
    while(i<=9){
      printf("%d x %d = %d\n",n,i,n*i);//   입력한숫자n과i1부터
                                         9까지넣어서곱한다고설정
      i+=1;
  }
}

main(){
      int num;
      scanf("%d",&num);              숫자를인식해서출력
      
      }
